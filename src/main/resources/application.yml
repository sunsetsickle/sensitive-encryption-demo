server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookmanger?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

# AES加密配置
encrypt:
  key: liang12345678910

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  type-handlers-package: com.example.demo.handler
  mapper-locations: classpath:mapper/*.xml
